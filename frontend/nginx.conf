server {
  listen 80;
  server_name localhost; # Or your domain

  root /usr/share/nginx/html;
  index index.html index.htm;

  location / {
    # Try files first, then directories, then fallback to index.html for SPA routing
    try_files $uri $uri/ /index.html;
  }

}